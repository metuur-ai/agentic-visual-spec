# GitHub PR-Based Collaborative Documents — Tasks

Source of truth: `docs/ears/github-pr-collaborative-documents.md` (acceptance IDs `R-x.y`) and `docs/lld/github-pr-collaborative-documents.md` (architecture, build order). Source paths below are relative to `packages/visual-spec/`.

**Build order.** Phase 0 pins the two invariants everything else assumes (`nodeId` survives a live editor; local mode does not regress). Then the foundation — protocol types and the owned-node layer — because both the store and the renderer depend on `nodeId` existing. The **renderer (Unit 7) is the critical path**: it is the largest single item, nothing about reviewing works without it, and it can start as soon as the node layer lands. The GitHub adapter (Unit 4) is fully independent and can run in parallel from day one.

**Parallel lanes once Phase 1 lands:**
- Lane A — renderer + reviewer UI (7.x, 11.x) ← longest, start first
- Lane B — GitHub adapter + comment projection (4.x, 5.x)
- Lane C — document store + jobs (3.x, 8.x)

**Do not touch** `ui/anchor-resolver.ts`, `ui/markdown-surface.tsx`, or the local comment path. Collaboration uses a separate resolver and a separate renderer by design (R-6.6, R-10.6); sharing them is what puts R-10.1 at risk.

---

## Phase 0: De-risk and safety net

- [x] 0.1 jsdom test harness + `nodeId` identity contract (est: ~1d)
  - why: the whole design rests on `nodeId` surviving a live editor. Probes proved Lexical honours `{replace, with, withKlass}` and that Luthor's provider flat-maps extension `getNodes()` into `initialConfig.nodes` — but nobody has mounted the real preset and typed into it. If this fails, Unit 2 needs a third design and the plan below is void.
  - acceptance: R-12.1 — a node created by a live editor mount retains its `nodeId` through `getJSON()` / `injectJSON()`.
  - verify: add `jsdom` + `@testing-library/react` as devDeps; mount `ExtensiveEditor` with a registered replacement node; type, split a block, merge two blocks, paste, reload from JSON; assert `nodeId` stable per surviving block.
  - landed: `packages/visual-spec/ui/node-identity.contract.test.tsx` (new, 13 tests), `packages/visual-spec/vitest.config.ts` (`.tsx` includes, `resolve.dedupe: ['lexical']`, inline luthor/lexical deps), `packages/visual-spec/package.json` (devDeps `jsdom`, `@testing-library/react`, `@testing-library/dom`). VERDICT: identity contract HOLDS, but **not via the LLD's mechanism** — see the test file header and the LLD §2 amendments below.
  - findings that amend LLD §2 / EARS R-2.2, R-2.3:
    1. **R-2.2 and R-2.3 are mutually exclusive on Lexical 0.40.** `LexicalNode`'s constructor calls `errorOnTypeKlassMismatch` and the node registry is keyed by type string, so a replacement subclass whose `getType()` returns `'paragraph'` throws on every construction; `exportNodeToJSON` additionally rejects an `exportJSON().type` that differs from `getType()`. Node replacement therefore forces a distinct serialized `type` and `MARKDOWN_SUPPORTED_NODE_TYPES` stops matching. This is NOT the missing-`withKlass` failure the LLD predicted (registration verified correct).
    2. **Use Lexical NodeState instead** (`createState` / `$setState` / `$getState`, serialized under `$`). It carries `nodeId`, keeps `type: 'paragraph'`, needs no subclass, and round-trips through `importJSON` for free. Unit 2.1 should be re-scoped to this.
    3. **`markdownSourceOfTruth` makes `getJSON()` lossy** — Luthor implements it as `markdownToJSON(getMarkdown())`, wiping every id. The collaboration mount must not set it (`ui/wysiwyg-editor.tsx:402` does today).
    4. **`injectJSON` does not run node transforms** (`setEditorState` replaces the node map wholesale) and is deferred behind a 100ms `setTimeout`. R-2.8 backfill is load bearing on every load, not only for legacy documents.

- [x] 0.2 Local-mode regression suite (est: ~4h)
  - why: Unit 10 is the "worse than not shipping" risk and nothing currently asserts local mode is unchanged. This has to exist *before* shared UI is touched, not after.
  - acceptance: R-12.5 — regression suite pinning local-mode comment resolution; R-10.1 through R-10.6 — browsing, viewing, commenting, apply, and legacy `{file, anchor}` upgrade all behave as today with no collaboration config present.
  - verify: suite passes on `main` before any collaboration code lands, and stays green at every subsequent story.
  - landed: `core/editing/local-mode.regression.test.ts` — 70 characterization tests covering R-10.1..R-10.6 + R-12.5 (browsing, source/markdown surface, `/__vs/comments` add/edit/delete, sidecar on-disk format, apply-prompt, `resolveMarkdownAnchors` degraded cases, legacy `{file, anchor}` upgrade, GitHub-free assertions, and an inventory of the pre-existing guards). Tests only, no production change. Suite 153 → 223 green. Quirks pinned deliberately (not fixed): PATCH with no `status` blanks the field; `kind:"file"` + `startLine` drops the line; `selection:"range"` writes an empty `endSnippet`; the heading fallback in `resolveMarkdownAnchors` sweeps siblings using the stale `startLine`; the legacy upgrader appends `.md` to any extension.

- [x] 0.3 Bundle guard — no React in Node-reachable output (est: ~2h)
  - why: `@lyfie/luthor-headless` bundled by esbuild inlines `react-dom/server`, producing a 3.8 MB artifact that throws `Dynamic require of "react-dom/server.node.js"` at import. A single stray import from `core/` ships a broken binary.
  - acceptance: R-12.6 — every Node-reachable bundled entrypoint (CLI and Vite plugin host) contains no `react`/`react-dom` references; R-3.3 — no module reachable from the CLI entrypoint imports Luthor.
  - verify: build, then grep `dist/cli.js` and the Vite host bundle for `react`; add `@lyfie/luthor*` to `tsup.config.ts` `external`; assert the check fails when a deliberate import is added.
  - landed: three layers. (1) STATIC — `core/bundle-guard.test.ts` walks the TS import graph from `src/cli.ts`, `core/vite/index.ts`, `core/index.ts` and asserts none reaches `react`/`react-dom`/`@lyfie/luthor`; runs in `npm test`. (2) BUILD — `build.mjs` installs an esbuild `forbid-browser-deps` `onResolve` plugin that *rejects* (does not externalize) those specifiers, so the CLI fails to build rather than deferring to a runtime require; `tsup.config.ts` externalizes `/^@lyfie\/luthor/`. (3) OUTPUT — `npm run check:bundle` (`check-bundle.mjs`) greps `dist/cli.js`, `dist/vite/index.js`, `dist/index.js` plus their tsup chunks; run it after `npm run build` (`dist/ui/**` is deliberately excluded — the browser bundle should contain react). Proof: a deliberate `import '@lyfie/luthor'` in `src/server.ts` + `core/vite/md-plugin.ts` failed all three, then was removed. Baseline had no leak — `dist/cli.js` was and remains 58,235 bytes with 0 react/luthor hits.

---

## Phase 1: Foundation

## Unit 1: Collaboration protocol model

- [x] 1.1 Add `core/collaboration/document-protocol.ts` (deps: 0.1, est: ~4h)
  - why: every later layer needs one typed vocabulary for document, node, anchor and GitHub identity. Inventing these per call-site is how the layers drift.
  - acceptance: R-1.1 — new module, separate from `core/editing/comment-doc.ts`; R-1.2 — document carries `documentId`, `documentPath`, `title`, `frontmatter`, `nodes[]` with frontmatter on the envelope not inside `JsonDocument`; R-1.3 — node carries `id`/`type`/`version`/`content`; R-1.4 — anchor carries `nodeId`, `nodeVersion`, `github`; R-1.5 — GitHub binding carries `owner`, `repo`, `branch`, `pullNumber`, `headSha`, `issueCommentId`, `replyToId`, `resolved`; R-1.6 — collaborative target carries `documentId` + `nodeId`.
  - verify: type-level tests; assert `CommentTarget` is byte-identical to today (R-1.7); assert unknown fields survive a read/write round-trip (R-1.8).
  - landed: `packages/visual-spec/core/collaboration/document-protocol.ts` + `document-protocol.test.ts` (12 tests). Types only plus `parseCollaborationDocument` / `serializeCollaborationDocument` (R-1.8) and `resolveDocumentTitle`. No runtime imports — `JsonDocument` is a local structural `{ root }` type, so nothing on the CLI path pulls in Luthor or react (R-3.3 / R-12.6). Title precedence decided: `frontmatter.title` wins; the envelope `title` is a derived cache refreshed on write (LLD Open Questions updated).

## Unit 2: Owned node layer

- [x] 2.1 NodeState-based `nodeId` + per-class transforms (deps: 0.1, 1.1, est: ~1w)
  - **RE-SCOPED after task 0.1.** The original title was "Owned block node classes carrying `nodeId`". Task 0.1 proved node replacement cannot keep the base `getType()` on Lexical 0.40, so R-2.2 now mandates Lexical's NodeState API instead. See the amended R-2.2 note in the EARS spec and the superseded-mechanism note in LLD §2.
  - why: serialized Lexical nodes have no stable key. A node transform registered per concrete class is the interception point that covers nodes created *inside* the editor — on Enter, on paste, on list toggle — and NodeState is the only carrier that survives serialization without changing the node's `type`.
  - acceptance: R-2.2 — `nodeId` carried via `createState`/`$setState`/`$getState`, assigned by `registerNodeTransform` per concrete class, not a structural path or sidecar map; R-2.3 — the serialized `type` stays the base string so `MARKDOWN_SUPPORTED_NODE_TYPES` and the transformers keep matching (free under NodeState); R-2.4 — every editor-created node gets a document-unique `nodeId`; R-2.5 — `nodeId` survives `exportJSON()`/`importJSON()`.
  - verify: extend the 0.1 harness (`ui/node-identity.contract.test.tsx`) across every block type in `MARKDOWN_SUPPORTED_NODE_TYPES` (R-12.2). Note `registerNodeTransform` rejects abstract bases like `ElementNode` — one call per concrete class.
  - landed: `ui/node-id-extension.ts` (new) + `ui/node-id-extension.contract.test.tsx` (new, 22 tests). Suite 397 → 419 green, `tsc --noEmit` clean. Exports `nodeIdState`, `NODE_ID_BLOCK_TYPES`, `NODE_ID_EXCLUDED_TYPES`, `NODE_ID_UNSERIALIZABLE_TYPES`, `$getNodeId`, `getSerializedNodeId`, `createRandomNodeId`, `resolveNodeIdTransformClasses`, `registerNodeIdTransforms`, `createNodeIdExtension`. Decisions:
    1. **Block types are derived, not hardcoded** — `NODE_ID_BLOCK_TYPES = MARKDOWN_SUPPORTED_NODE_TYPES − NODE_ID_EXCLUDED_TYPES`, where the exclusions (`root`, `text`, `linebreak`, `tab`, `code-highlight`, `link`, `autolink` — root plus inline nodes) each carry a stated reason. A block type added by a future Luthor release joins the covered list automatically and `resolveNodeIdTransformClasses` throws if it cannot be resolved, so it can never be silently skipped (R-12.2).
    2. **Transform classes are resolved from the editor's node registry** (`editor._nodes.get(type).klass`), not imported: `image`, `iframe-embed` and `youtube-embed` have no stable named export, and this guarantees the transform binds the exact class the editor uses. One `registerNodeTransform` per concrete class, as 0.1 required.
    3. **Uniqueness is enforced against a live `nodeId → nodeKey` index, not "assign when absent."** Lexical's `application/x-lexical-editor` clipboard payload carries the `$` state, so pasting blocks copied from the same document reintroduces ids that are still live. The **copy** is re-issued a fresh id and the node that already held it keeps it, so comments already anchored stay anchored. The index is rebuilt from the document on the first transform of each update, because `injectJSON` swaps the node map without running transforms.
    4. **Default generator is random, not a counter.** A counter restarts at 1 on every reload while the loaded document already holds `n1…`; the round-trip test pins that a fresh session's generator never re-issues a live id.
    5. **`nodeId` lands under the `$` key** — `{"type":"paragraph", …, "$":{"nodeId":"…"}}`. Readers must use `getSerializedNodeId(node)` (`node.$.nodeId`), **not** `node.id` / `node.nodeId`; task 3.1's `resolveNode` matches on `id`/`nodeId` and needs adjusting to read `$`.
  - **NEW LIMITATION found (affects 2.3, 7.1, 7.4, 11.x):** `image`, `iframe-embed` and `youtube-embed` get a `nodeId` in memory but it **does not survive serialization**. Luthor 2.9's node classes build `exportJSON()` as an object literal instead of spreading `super.exportJSON()` — and `LexicalNode.exportJSON()` is where Lexical writes the `$` key — and their `importJSON()` constructs a fresh node rather than going through `updateFromJSON`, so neither direction round-trips. Lexical's own decorator nodes are unaffected (`horizontalrule` round-trips). Recorded as `NODE_ID_UNSERIALIZABLE_TYPES`; the R-12.2 test pins both halves (covered by a transform, gap in serialization) so it fails the day Luthor fixes it. Consequence: an image or an embed cannot be a durable comment target yet.

- [x] 2.2 Version bump + backfill (deps: 2.1, est: ~3h)
  - why: `version` is what distinguishes "same block, edited since the comment" so a comment can be flagged outdated. Backfill covers documents authored before this shipped and any node that escapes the replacement.
  - acceptance: R-2.6 — content change bumps `version`, never `nodeId`; R-2.7 — unchanged content does not bump; R-2.8 — missing `nodeId` backfilled on first write and recorded; R-2.12 — a write that cannot produce unique ids fails rather than persisting a partially identified document.
  - verify: property test over random edit scripts (insert/delete/move/edit) asserting id stability and version monotonicity.
  - landed: `core/collaboration/node-identity.ts` (new) + `core/collaboration/node-identity.test.ts` (new, 25 tests) + `ui/node-identity-parity.test.ts` (new, 2 tests). Suite 533 → 560 green, `tsc --noEmit` clean. Exports `NODE_IDENTITY_EXCLUDED_TYPES`, `isAddressableBlockType`, `collectBlocks`, `nodeContentSignature`, `nodeTextContent`, `NodeIdentityError`, `NodeIdentityRecord`, `NodeIdentityBackfillRecord`, `ReconcileOptions`, `ReconcileResult`, `reconcileDocumentIdentity`, `withNodeIdentity`. Decisions:
    1. **Pure JSON, so it lives in `core/collaboration/`** — nothing here needs a `LexicalEditor`; a serialized node is a plain object, `nodeId` is `node.$.nodeId`, and content comparison is structural. Under `ui/` it would be unreachable from the CLI and the Vite host, which is where writes happen.
    2. **"Content" = the block's whole serialized subtree** — text, formatting marks, attributes and child structure — canonicalized with keys sorted and every `$` key stripped at every depth. Formatting counts (bolding the commented sentence changes what the reviewer sees); identity does not (which is why a backfill can never bump the version it just assigned an id for). A parent's signature contains its children's, so editing a list item bumps both the item and the list.
    3. **Version bumping runs at the store/serialization boundary, not as an editor transform.** A transform fires on every editor update — caret moves, selection changes, re-renders — so it would inflate `version` and flag anchored comments outdated for edits nobody made. The boundary compares exactly two persisted revisions.
    4. **`version` lives on the envelope's `nodes` projection** (`CollaborationNode.version`, LLD §1), not inside the Lexical tree: a field added to the tree is either dropped by the next `importJSON` or, under `$`, editable from inside the editor. `reconcileDocumentIdentity` rebuilds the projection every time and is the single place `version` is defined.
    5. **The backfill record is an envelope field, `identity.backfill = { at, nodeIds }`**, cumulative. It is a durable property of the document, not of a run: a machine-assigned id is not the id any earlier comment anchored to, and a consumer investigating an orphan needs the whole set, not the last batch. A job event would be gone by the time anyone asks. The record is only rewritten when something was actually backfilled, so a reconcile on every load does not churn the file.
    6. **Backfill does not churn `version`** — the content signature ignores `$`, so assigning an id changes no signature; and a newly identified block has no counterpart in the previous revision, so it starts at `version: 1` rather than bumping. Second pass over the same document is a no-op (test pins it).
    7. **R-2.12 fails, it does not repair.** Two live blocks sharing a `nodeId` throw `NodeIdentityError` — at this boundary there is no way to tell which one a comment was anchored to, and `registerNodeIdTransforms` already de-duplicates inside the editor where the copy *is* identifiable. A generator that cannot produce an id unique against the live document within 50 attempts throws too. `withNodeIdentity(store).write` throws before `store.write` is called, so nothing partially identified is persisted (test asserts the inner store is never touched).
    8. **`withNodeIdentity(store)` is the wiring point** — a decorator over any `DocumentStore` so the same rules hold for the GitHub-backed store (3.2). `read` backfills in memory and does not write back (a read that commits is unacceptable on a PR branch; backfill is a repair mechanism, LLD §2), `write` bumps against the previously persisted revision. `fsDocumentStore` itself is unchanged.
    9. **The exclusion list is stated twice on purpose.** `core/` cannot import `ui/node-id-extension.ts` (it pulls Luthor, R-3.3), so `NODE_IDENTITY_EXCLUDED_TYPES` mirrors `Object.keys(NODE_ID_EXCLUDED_TYPES)`; `ui/node-identity-parity.test.ts` is the only file allowed to import both and fails if they drift.
    10. **Property test**: 200 seeded cases (mulberry32, seeds 1…200, no `Math.random()`) × 12 random ops from insert / delete / move / edit / no-op, over paragraphs, headings and quotes-with-nested-paragraphs. Logical identity is tracked out-of-band through a `$.marker` (stripped from the signature) so id stability cannot be true by construction. Per step it asserts: every block identified and no id shared; the projection covers every block; ids of surviving blocks unchanged across insert/delete/move/edit; `version` never decreases; `version` increments **exactly** when the content signature changed and is otherwise unchanged; `bumpedNodeIds` agrees with the signatures; new blocks start at 1; and every backfilled id appears in `identity.backfill.nodeIds`. On failure it prints the seed and the operation log. Killed three deliberate mutants (never bump, always bump, always re-issue ids).

- [x] 2.3 Publish payload generator (deps: 2.1, est: ~4h)
  - why: `json` and `markdown` must come from **one** object at **one** instant. The server treats the Markdown as opaque, so no server-side check can catch a client that serialized the two from different states — this invariant is the only thing preventing a silent mismatch.
  - acceptance: R-2.9 — uses `jsonToMarkdown(doc, { metadataMode: 'none' })`; R-2.10 — generated Markdown is write-only, never parsed back; R-2.11 — editor driven through `getJSON()`/`injectJSON()`, not a Markdown string buffer.
  - verify: R-12.8 — test asserts both artifacts derive from the same document object via a single call. Explicitly **not** reusing `normalizeForStore(getMarkdown())` (`ui/wysiwyg-editor.tsx:135`), which reads live editor state and applies local-viewer image rewriting.
  - landed: `ui/publish-payload.ts` (new) + `ui/publish-payload.test.ts` (new, 11 tests). Suite 533 → 544 green, `tsc --noEmit` clean. Exports `generatePublishPayload`, `PublishPayload`, `PublishDocumentSource`, `DroppedNodeReport`. Decisions:
    1. **The function takes a reader, not a document, and calls it exactly once.** `generatePublishPayload(() => editorRef.current.getJSON())`. Taking a reader is what makes the single-instant invariant testable: the R-12.8 test rigs a source that returns a *different* document on every call, so a hypothetical second read of live editor state would surface as `markdown` describing a document `json` does not. A signature that took a document object could only ever assert "both fields exist".
    2. **The one read is immediately detached** — `JSON.parse` for the `getJSON()` string, `structuredClone` for a document object — so an edit landing after the call reaches neither artifact. `markdown` is then always exactly `jsonToMarkdown(payload.json, { metadataMode: 'none' })`, which is verifiable after the fact.
    3. **Dropped nodes are surfaced, not silently lost.** `prepareDocumentForBridge(json, { mode: 'markdown', supportedNodeTypes: MARKDOWN_SUPPORTED_NODE_TYPES })` is used for **detection only**: its envelopes become `payload.droppedNodes` (`{ nodeId?, type, path, fallback }`, `nodeId` read via `getSerializedNodeId` off the `$` key) and are never appended to the output. Verified empirically that the `$` key itself produces no envelopes, so `nodeId` never pollutes the report.
    4. **`normalizeForStore(getMarkdown())` is not reused** for two independent reasons: it is a *second* read of live editor state (the divergence this task exists to prevent), and it rewrites image `src`s for the local viewer, which is meaningless in a published artifact.
    5. **The three unserializable types are a non-issue here.** `image`, `iframe-embed` and `youtube-embed` lose their `nodeId` on export, but publishing never reads `nodeId`, and all three are in `MARKDOWN_SUPPORTED_NODE_TYPES` so they are never dropped. A test pins that membership.

- [x] 2.4 Import-boundary lint for the collaboration path (deps: 2.3, est: ~2h)
  - why: `markdownToInjectable` does not throw on an id-less tree — it returns a structurally valid document with every `nodeId` gone. Silent identity loss is the worst failure mode in the design, so convention is not enough.
  - acceptance: R-2.13 — no collaboration module imports `markdownToInjectable()` or `canonicalizeMarkdown()`, asserted by a test.
  - verify: lint rule fails when a deliberate import is added to a collab module.
  - landed: `core/collaboration/import-boundary.test.ts` (new, 6 tests) + `core/import-graph.ts` (new). Suite 533 → 539 green, `tsc --noEmit` clean. Decisions:
    1. **The collaboration path is enumerated by pattern, not by file list**, so a module added by a concurrent task is covered the moment it lands: `core/collaboration/*.tsx?`, `core/vite/routes/collab*.tsx?`, `ui/(node-id|publish|collab)*.tsx?`. The `ui/` boundary is a name prefix rather than a directory because the collaboration UI shares `ui/` with the local viewer, and the local viewer legitimately parses Markdown — `ui/wysiwyg-editor.tsx` imports `markdownToInjectable` on its load path and must keep doing so. A "guards against a vacuous pass" test pins four anchors that exist today, so a layout change that makes the patterns match nothing fails loudly instead of passing on an empty set.
    2. **The 0.3 walker is shared, not duplicated.** `specifiersOf` / `resolveRelative` / the BFS moved verbatim out of `core/bundle-guard.test.ts` into `core/import-graph.ts`; bundle-guard's assertions are byte-identical, only its private helper changed. 0.3 asks "does this graph reach a forbidden *package*", 2.4 asks "does it reach a forbidden *binding*" — same traversal, different predicate.
    3. **Forbidden by binding, not by module.** `mapImages` / `normalizeForStore` in `ui/luthor-bridge.ts` are pure string work and stay reachable; the banned names are `markdownToInjectable`, `canonicalizeMarkdown` and `markdownToJSON` (Luthor's primitive, added per LLD §2 "No-Markdown-read check"). Detection covers named imports *and* `.name(` call sites, closing the `import * as bridge` escape hatch.
    4. Assertions compare a joined string rather than an array so the failure diff prints the offending chains in full instead of vitest's `[ …(6) ]` elision.
  - proof (required, then reverted): a deliberate `import { markdownToInjectable } from '../../ui/luthor-bridge'` in `core/collaboration/document-protocol.ts` failed two of the six tests, naming every chain — e.g. `core/vite/routes/collab.ts → core/collaboration/document-protocol.ts: imports \`markdownToInjectable\` from '../../ui/luthor-bridge'` and the transitive `core/collaboration/job-hub.ts → core/collaboration/document-store.ts → core/collaboration/document-protocol.ts → ui/luthor-bridge.ts: calls \`.markdownToJSON()\``.
  - finding: **nothing on the collaboration path reaches those bindings today.** `ui/wysiwyg-editor.tsx` is the only importer of `markdownToInjectable` in the package and is deliberately off the path; `canonicalizeMarkdown` has no importer outside `ui/luthor-bridge.test.ts`.

---

## Phase 2: Parallel lanes

## Unit 3: Document store — Lane C

- [x] 3.1 `DocumentStore` interface + local implementation (deps: 1.1, est: ~3d)
  - why: structured JSON does not fit `SurfaceStore`, whose read/write/list contract assumes text. Keeping Markdown *out* of this interface is deliberate — generation happens in the browser, so the server never needs a serializer.
  - acceptance: R-2.1 — canonical document persisted as a Luthor `JsonDocument`, no bespoke node schema; R-3.1 — read/write/list; R-3.2 — **does not** render or generate Markdown; R-3.4 — resolve `nodeId` → JSON location; R-3.5 — local file-backed impl at `documents/<documentId>.json`; R-3.7 — collaboration JSON never routed through `SurfaceStore`; R-3.8 — unresolved `nodeId` reported, not thrown.
  - verify: unit tests; assert the interface has no Markdown surface.
  - landed: `core/collaboration/document-store.ts`, `core/collaboration/document-store.test.ts` (14 tests). `read()` resolves `null` for an absent document, mirroring `GitHubAdapter.getFile`, so 3.2 maps cleanly. `resolveNode` returns a discriminated `NodeResolution` — `{ found: false }` or `{ found: true, path, node }` — where `path` is the child-index route from `doc.root` (R-3.4); the pure `resolveNodeIn(doc, nodeId)` is exported for 6.1 so anchor resolution needs no re-read. Persistence goes through 1.1's `parseCollaborationDocument` / `serializeCollaborationDocument`, so unknown fields survive the store (R-2.1 / R-1.8). Suite 263 → 277.

- [x] 3.2 GitHub-backed `DocumentStore` (deps: 3.1, 4.1, est: ~4d)
  - why: the same operations mapped onto a PR branch, so the routes are backend-agnostic.
  - acceptance: R-3.6 — GitHub impl maps read/write/list onto a PR branch.
  - verify: exercised against the recorded-response executor from 4.1; no live repo needed.
  - landed: `core/collaboration/github-document-store.ts` — `githubDocumentStore(adapter, { repo, branch, documentsDir? })`, same `<documentsDir>/<documentId>.json` layout as 3.1 so a document is portable between backends. Resolution reuses 3.1's `resolveNodeIn` / `DOCUMENT_ID_RE`; persistence reuses 1.1's parse/serialize. **Backend-agnosticism is proved, not asserted**: `core/collaboration/document-store.contract.test.ts` runs one 11-test suite against *both* stores (22 tests), the GitHub one driven by a fake `gh` executor holding an in-memory branch. `core/collaboration/github-document-store.test.ts` (20 tests) covers the GitHub-specific half from recorded fixtures. **Adapter gap closed** with a minimal additive `listFiles(repo, path, ref)` on `GitHubAdapter` — the Contents API returns an array for a directory path, so `list()` is the same endpoint as `read()`; no manifest file, which would be derived state able to drift from the branch that is the system of record. **Write is read-before-write**: `getFile` on the branch supplies the blob sha, present ⇒ PUT with `sha` (update), absent ⇒ PUT without (create). The read/PUT pair is not atomic and cannot be over this API; a lost race surfaces as a typed `DocumentWriteConflictError` (409 stale sha, or 422 `"sha" wasn't supplied` on the create side) carrying `documentId`/`path`/`branch`/`expectedSha`/`cause` — the `DocumentStore` shape is unchanged, since `write()` returns `void` and offers no return channel. The store detects and refuses; it never retries — resolution is R-8.x's. Suite 277 → 319.

## Unit 4: GitHub adapter — Lane B (independent, start day 1)

- [x] 4.1 REST adapter with injectable executor (deps: none, est: ~4d)
  - why: shelling out to `gh` avoids reimplementing auth, pagination and rate limiting, and lets the Claude CLI drive the same operations. The injectable executor is what makes any of it testable — mirror the `spawnClaude` seam at `core/vite/routes/apply.ts:158`.
  - acceptance: R-4.1 — single adapter over `gh`/MCP; R-4.2 — no bespoke HTTP/GraphQL client; R-4.3 — branch, commit, open PR; R-4.4 — list/create/update/delete **issue** comments; R-4.5 — paginate the full comment list; R-4.6 — REST only, no GraphQL dependency; R-4.7 — merge; R-4.8 / R-12.3 — injectable process-spawn or request executor, mirroring `spawnClaude`; R-4.9 — structured errors carrying no credential material.
  - verify: recorded `gh api` fixtures replayed through the executor. Commit via the Contents API only — a shell-out to `git commit` applies `.gitattributes` CRLF normalization and breaks publish verification permanently.
  - landed: `core/collaboration/github-adapter.ts`, `core/collaboration/github-executor.ts`, `core/collaboration/github-adapter.test.ts` (24 tests), `core/collaboration/fixtures/*.json` (11 recorded responses). Pagination is an explicit `page=` loop, not `Link`-following: the executor is buffered and exposes stdout only, so response headers are not observable. Suite 153 → 177.

- [x] 4.2 Credential + scope preflight (deps: 4.1, est: ~4h)
  - why: the real credential on a dev machine is a keyring OAuth token, not a PAT in an env var, and it may lack scopes. Today the first symptom would be a raw 403 mid-publish.
  - acceptance: R-9.1 — read credential server-side from env or `gh` auth state; R-9.2 — credential never reaches the browser, a response body, an SSE event, or the client bundle; R-9.3 — credential never written to logs, job status, or error messages; R-9.4 — accepts owner/repo/base-branch configuration; R-9.12 — verify required scopes on enable and name the specific missing scope; R-4.10 — unavailable execution path reported as unavailable, with no partially-functional fallback; R-9.19 — no credential ⇒ collaboration disabled, local mode unaffected.
  - verify: simulate missing scope and assert the message names it.
  - landed: `core/collaboration/credentials.ts` (`preflightCollaboration`), `core/collaboration/credentials.test.ts` (17 tests), `core/collaboration/fixtures/user-inclusive-*.txt` (3 recorded responses), plus `collaboration` owner/repo/baseBranch config in `core/config.ts`. Scopes and identity come from one `gh api -i /user` call: `-i` puts `X-OAuth-Scopes` on stdout as machine-readable text, which the buffered executor can see, whereas `gh auth status` prints a human-formatted table whose stream and wording have moved across `gh` releases and which cannot resolve the identity in the same call. The module never reads a token *value* — env vars are probed for presence only — so the success shape carries identity + scopes + availability and no secret. Suite 263 → 280.

## Unit 5: Comment projection — Lane B

- [x] 5.1 Issue-comment projection into `CommentDoc` (deps: 4.1, est: ~4d)
  - why: `/__vs/comments` and the apply hub already depend on the `CommentDocStore` interface rather than on files, so projecting through it keeps both working. But `write(doc)` is a whole-document snapshot swap returning `void` — there is no channel for the created comment's id, so the seam needs intent-based methods.
  - acceptance: R-5.1 — projection of PR issue comments into `CommentDoc`; R-5.2 — GitHub is the system of record; R-5.3 — sidecar is a non-authoritative cache; R-5.4 — created comment carries a machine-readable `documentId`+`nodeId` trailer and persists the returned id; R-5.5 — **no** review comments created; R-5.6 — comments authored on github.com appear after sync, including ones with no `nodeId`; R-5.7 — comments without a `nodeId` land in a document-level discussion view.
  - verify: fixture-replayed round-trip; assert `addComment` returns the GitHub id.
  - landed: `core/collaboration/comment-projection.ts` + `comment-projection.test.ts` (22 tests) + `fixtures/projection-comments.json`. **Trailer format (parsed by 5.2 / 5.3 / 6.1 / 8.x):** the body is the author's text, a blank line, then a single-line HTML comment as the last line — `<!-- visual-spec: documentId=doc-1 nodeId=n-7 -->`. Keys are `[A-Za-z][A-Za-z0-9]*`, values `encodeURIComponent`-encoded so no space/newline/`-->` can appear inside one; unknown keys survive a parse/format round-trip, so 5.2 can add `resolved=`/`replyTo=` and 5.3 a `key=` with no format change. `nodeId` is omitted for document-level comments; a body with no trailer (authored on github.com) is preserved verbatim. **Seam:** `CommentDocStore` gains three OPTIONAL intent methods — `addComment?`, `updateComment?`, `deleteComment?` — plus a `CommentPatch` type; `fileCommentStore` implements all three via its existing snapshot read/modify/write (the PATCH "status blanks when absent" quirk is preserved deliberately), and `handleCommentsRequest` uses them when present and falls back to the snapshot path when not, so the in-memory stores in the existing suites stay valid unchanged. Record ids are derived from the GitHub comment id (`c-<hex>`, reversible) so a sync holds no local id map and PATCH/DELETE route matching is unchanged. `read()` always hits GitHub and never the cache (R-5.2/R-5.9); `write(doc)` only mirrors into the optional non-authoritative cache and never touches GitHub (R-5.3). Status/resolution is deliberately NOT projected to GitHub here — that is 5.2. Suite 263 → 285.

- [x] 5.2 Resolution-state convention (deps: 5.1, est: ~2d)
  - why: issue comments have no native resolve, and reviewers cannot push — so resolution has to be expressible as a comment or it cannot exist at all.
  - acceptance: R-5.12 — resolution is a reply comment carrying a machine-readable resolved marker; R-5.13 — posted as the acting user's own identity; R-5.14 — state derived from the latest marker in replies, never from the local cache; R-5.15 — legible on github.com without visual-spec.
  - verify: resolve/unresolve round-trip through fixtures; assert cache is not consulted.
  - landed: `core/collaboration/comment-projection.ts` (additive) + `fixtures/resolution-replies.json` + `fixtures/resolution-reply-create.json`. **Convention:** resolution is a reply issue comment reusing the 5.1 trailer — two new keys, `replyTo` (the parent's GitHub issue-comment id; issue comments are a flat list with no native threading, so the parent link has to be carried in the body) and `resolved` (`true`/`false`). Exact bodies: `Resolved this comment: <parent htmlUrl>` / `Reopened this comment: <parent htmlUrl>`, blank line, then `<!-- visual-spec: documentId=doc-1 replyTo=700001 resolved=true|false -->`. GitHub hides the trailer, so the visible line is a plain sentence plus a link straight to the comment it is about (R-5.15). **Markers accumulate** — an unresolve posts a second reply and never edits the first, because editing needs the other participant's token and would erase the history the convention exists to provide. **Ordering for "latest" (R-5.14):** `createdAt` ascending (GitHub-assigned ISO-8601 UTC, so lexicographic = chronological), tie-broken on the higher GitHub comment id — `createdAt` has one-second granularity so ties are real. **Derived state lands on `ProjectedCommentRecord.resolved?: boolean`** (absent = never toggled, which is distinct from an explicit unresolve) and is mirrored onto `status` (`applied` when resolved) so the existing `openByPath`/`openByWorkflow` groupings drop resolved comments unchanged. Derivation (`withResolutionState`) runs over the list `read()` just fetched from GitHub — the cache is never consulted. **API:** `formatResolutionReply`, `resolutionByComment`, `withResolutionState`, `isResolutionReply`, and `githubCommentStore` now returns `GitHubCommentStore` = `CommentDocStore` + `setResolved(id, resolved)`, which posts through the ordinary `createIssueComment` path so GitHub attributes the reply to whichever credential the acting instance runs with (R-5.13) and no identity is ever written into the body. Resolution replies stay in `doc.comments` (nothing discarded) but are excluded from `documentDiscussion` — they belong to their parent's thread. `updateComment(status)` is deliberately still a no-op; the route wiring is 7.2. Suite 316 → 331.

- [x] 5.3 Cache lifecycle + apply-flow target (deps: 5.1, est: ~1d)
  - why: `apply-prompt.ts:16` currently hardcodes the sidecar as source of truth, which contradicts R-5.3. And R-5.8 deletes the cache on merge — so anything living only in the cache is destroyed exactly when the changelog needs it.
  - acceptance: R-5.8 — cache deleted on merge; R-5.9 — GitHub wins on disagreement; R-5.10 — apply flow takes a mode parameter selecting the canonical JSON as edit target and never edits generated Markdown; R-5.11 — comment creation retry is idempotent.
  - verify: assert the apply prompt names the JSON document in collab mode and the sidecar in local mode.
  - landed: `core/collaboration/cache-lifecycle.ts` + `cache-lifecycle.test.ts` (9 tests), `core/editing/apply-prompt.ts` (mode parameter) + 7 tests, `core/collaboration/comment-projection.ts` (additive) + 5 tests + `fixtures/idempotent-create.json`. **Apply mode (R-5.10):** `buildApplyPrompt(open, options: ApplyPromptOptions = {})` where `ApplyPromptOptions = { mode?: 'local' } | { mode: 'collab'; documentPath: string }` — `documentPath` is required in collab mode because an agent told "edit the canonical JSON" without being told which file will guess, and guessing wrong means editing generated Markdown. The default is local and produces a byte-identical prompt, so `routes/apply.ts` and `ui/main-header.tsx` are unchanged and `local-mode.regression.test.ts` needed no edit. Collab mode swaps the instruction paragraph (canonical JSON as the only editable file, generated Markdown explicitly forbidden, sidecar demoted to cache, targets located by `nodeId` with no snippet/line fallback) and replaces the per-comment `Where:`/`Context:` lines with `Node: <nodeId>` — printing snippet data the instruction just disowned would invite the drift the collab anchor design exists to remove. **The prompt names no remote service:** R-10.5 scans this module's source and everything the agent needs is expressible as "which document to edit, which file not to trust", so where the conversation lives stays the caller's business. **Idempotency (R-5.11):** trailer key `key` (reserved by 5.1). `addComment` does a read-before-write — list the PR's comments, and if one already carries this key return THAT record instead of posting again. The key must be supplied by the caller and stable across retries of one logical create; a key generated inside `addComment` would differ per attempt and dedupe nothing. With no key the original single-POST path is unchanged, so no cost is added to the common case. `findByIdempotencyKey` breaks ties on the lowest comment id, so even a duplicate that did slip through converges. **Race window:** issue comments have no server-side idempotency and no compare-and-set, so a duplicate remains possible for the duration of one list→create round trip — classically when the first attempt's POST lands after the client timed out but before the retry's list runs, or when two attempts list concurrently. Detectable after the fact (two comments sharing a `key`), not preventable client-side. **R-5.8 ordering:** `mergeAndDropCommentCache` reads the conversation from GitHub *before* merging, merges, then deletes the cache *only if the merge actually merged*, and returns the pre-merge snapshot. The changelog therefore works from a value in hand rather than a file about to be removed, and since the cache only ever mirrors GitHub, nothing is lost when it goes. A refused merge leaves the cache intact. **R-5.9** needed no new reconciliation — 5.1/5.2 never make the cache an input, so disagreement is unobservable; `assertCacheNeverRead` wraps a cache so any future read throws instead of silently returning stale state. Suite 412 → 433.

## Unit 7: Renderer + collaboration UI — Lane A (critical path, start first)

- [x] 7.1 JSON → DOM renderer stamping node identity (deps: 2.1, est: ~3w)
  - why: **this does not exist and reviewers depend on it entirely.** The current render path is Markdown → react-markdown (`ui/markdown-surface.tsx:41`), which stamps `data-vs-loc` from mdast positions — but Markdown is `nodeId`-stripped, so ids cannot be stamped there. Without this component there is no review surface.
  - acceptance: R-7.3 — rendered blocks carry `data-vs-document-id`, `data-vs-node-id`, `data-vs-node-version`.
  - verify: render a fixture document; assert every block node has a `data-vs-node-id` matching the JSON; visual parity with the published Markdown (SC-10).
  - landed: `ui/collab-document-view.tsx` + `collab-document-view.test.tsx` (20 tests) + `ui/fixtures/collab-document.json`. `CollabDocumentView({ document: CollaborationDocument, resolveImageSrc?, className? })` walks `document.doc.root` recursively and renders it to DOM — no Markdown anywhere on the path. **The DOM contract (R-7.3):** the *outermost element of each block* carries `data-vs-document-id`, `data-vs-node-id` and `data-vs-node-version`; consumers locate a block with the exported `collabBlockSelector(documentId, nodeId)`. Ids are read with `getSerializedNodeId` (the NodeState `$` key), so the renderer and the store agree by construction. **Versions come from the `nodes` projection, never from the serialized node's own `version`** — that field is Lexical's node-class schema version and has nothing to do with content change (R-6.3 would mis-flag every comment if it were used). `data-vs-loc` is deliberately not emitted: the canonical JSON has no source positions, and R-7.3 lists it as optional. **Nothing is fabricated.** `image`, `iframe-embed` and `youtube-embed` reach the store without a `nodeId` (`NODE_ID_UNSERIALIZABLE_TYPES`); rather than synthesize one — which would differ on the next load and mis-anchor every comment on it — those blocks get `data-vs-uncommentable="<reason>"`, no id, and a visible badge (`data-vs-annotation="uncommentable"`, "no anchor — not commentable"). Same rule for a node the projection does not know: the version attribute is omitted, not guessed. **Unknown node types** render as a labelled, inert placeholder box that still shows the node's own children, and keep their identity attributes if they have an id — a reviewer seeing a marked gap is recoverable, a reviewer seeing silence reviews the gap as if it were the document. **Text formats:** all eight Lexical bits (bold/italic/strikethrough/underline/code/subscript/superscript/highlight), composed inner→outer deterministically; four of them are not Markdown-native, so the published artifact drops them while this surface still shows what is stored. **SC-10 parity is structural, not visual:** the fixture is rendered twice — once by this component from the JSON, once by react-markdown from `headless.jsonToMarkdown(doc)`, the exact published artifact — and the same normalizer reduces both DOMs to a *block outline* (ordered, nested block elements + leaf text) which must be equal. Three normalizations, each named in the test: non-block wrappers are transparent (`thead`/`tbody`/`figure`/`div`/`code`-in-`pre`), a `<p>` Markdown inserts inside a blockquote/li/cell or around a lone image is transparent there only, and `data-vs-annotation` chrome is stripped from text. It does **not** cover pixels, fonts, spacing, inline markup, or attributes, and cannot cover `iframe-embed`/`youtube-embed` (no Markdown form without the preset's extra transformers). Shared typography rides on the same `className="md"` root the local viewer uses. **R-10.6/R-6.6 hold with no edit to local mode:** `markdown-surface.tsx`, `anchor-resolver.ts` and the local comment path are untouched, and the module name matches the 2.4 import-boundary pattern so it can never reach `markdownToJSON`/`markdownToInjectable`/`canonicalizeMarkdown`. Suite 546 → 566.

- [x] 7.2 Collaboration routes (deps: 3.1, 5.1, est: ~3d)
  - why: an explicit route family keeps `/__vs/comments` semantics intact for local mode.
  - acceptance: R-7.1 — the `/__vs/collab/*` family incl. `POST /open`; R-7.2 — `/__vs/comments` unchanged; R-7.7 — no GitHub calls from the browser; R-7.8 — no collaboration controls without configuration.
  - verify: route tests; assert local comment routes are untouched.
  - landed: `core/vite/routes/collab.ts` + `collab.test.ts` (46 tests) + `collab.no-browser-github.test.ts` (8 tests). `createCollabRoutes(deps): CollabRouter` owns the whole `/__vs/collab/*` family (R-7.1) — `GET ''`, `POST /start`, `POST /open`, `GET /:id`, `GET /:id/events`, `POST /:id/sync`, `POST /:id/publish`, `POST /:id/comments`, `POST /:id/comments/:commentId/reply`, `PATCH /:id/comments/:commentId` — and **only** the route layer: it validates, resolves config/availability/document, then hands an injected `JobBody` to `jobs.hub(id).start()`. Both hosts (`src/server.ts`, `core/vite/md-plugin.ts`) do nothing but slice the `/__vs/collab` prefix and call `collab.handle({method, pathname, query, body, sse: res})`, so 7.5 has no host-specific logic to remove; each creates its own `createJobHubRegistry()` at server construction and disposes it on `close`. **R-7.8** is answered by `GET /__vs/collab` → `CollabAvailability` (`{available:false, reason:'not-configured'|<preflight reason>, message, missingScopes}` or `{available:true, repo, login, scopes}`), the single flag the UI reads before rendering any control; every GitHub-touching route returns **503 with that same payload** instead of 500 when collaboration is off. **R-8.9** payload validation (`missing json` / `missing markdown` → 400) is here, deliberately *ahead* of the availability gate so an incomplete payload is rejected as malformed even with no credential (R-12.7). Job bodies for create/open/sync/publish are injected via `bodies?: Partial<CollabJobBodies>` (factories `(input) => JobBody`); un-injected ones use a stub that **throws**, so the hub reports `job-error` rather than lying with `job-done ok:true` — 8.2 supplies `create`/`open`/`sync`, 8.3 supplies `publish`. Role enforcement is a named seam, not an implementation: `authorize?: CollabAuthorizer` is called on every route with a `CollabOperation` and the resolved `login`/`repo`, defaulting to allow-all — task 9.2 fills it in with no route changes. `/__vs/comments` is untouched (R-7.2): `handleCommentsRequest` is neither imported nor re-entered, asserted on the comment-stripped source, and the 0.2 regression suite stays green. R-7.7 is a source-level grep over `ui/**` + `core/app/**` for API hosts, Octokit, credential env vars/headers and non-same-origin `fetch(`, with live-pattern proofs against `github-executor.ts` / `credentials.ts`. R-9.13/R-9.15/R-9.16: tests assert the shared `checkRequest` guard is registered *before* the collab handler in both hosts by source position. Both hosts gained an optional `config?: VisualSpecConfig` so collaboration can be switched on; omitted, `resolveConfig()` yields `collaboration: null` and the family is inert. Suite 342 → 396.

- [x] 7.3 Wire comment panel + indicators to `nodeId` (deps: 7.1, 7.2, est: ~1w)
  - why: `ui/indicator-layer.tsx:20` and `core/app/lib/use-comments.ts:36` are **path-keyed**, and collaborative comments have no path. This is where R-10.1 is genuinely at risk — the change is to shared components local mode also uses.
  - acceptance: R-7.4 — existing `CommentPanel`/`IndicatorLayer` reused, not duplicated; R-7.5 — comments persisted against `nodeId` as primary identity.
  - verify: 0.2 regression suite stays green; collab indicators resolve by `nodeId`.
  - landed: `ui/indicator-layer.tsx` + `ui/comment-panel.tsx` (generalized, no fork), `ui/collab-comment-source.ts` (new) + `ui/collab-comment-source.test.tsx` (new, 15 jsdom tests), `core/vite/routes/collab.ts` (anchor capture at creation) + `core/vite/routes/collab-anchor.test.ts` (new, 7 tests), `ui/fixtures/virtual-surfaces.ts` + a `vitest.config.ts` alias (test-only). Suite 726 → 748, `tsc --noEmit` clean, `local-mode.regression.test.ts` **unedited and green (70)**.
    1. **One anchor-source seam per component, not an `if (collab)` branch.** `IndicatorLayer` gained an `IndicatorTarget` — `{ comments, title, ariaLabel, state?, element() }` — and a props union: `{path, mode}` (local) or `{targets}` (anything else). Placement and rendering are now one component, `Markers`; local mode's targets are built by `lineTarget()` from the *unchanged* `groupByStartLine` + `resolveMarkdownAnchors`/`resolveCodeAnchors` calls. `CommentPanel` gained a `CommentPanelSource` — which comments belong here, how a selection reads, what create does, which comments lost their target — with `localCommentSource` reproducing the old body verbatim (same `useComments(path)`, same `target.path === path` filter, same heading/line labels, same `add` payload). **Local mode's code path is identical:** the props both call sites pass are unchanged, the hooks it runs are unchanged, and the emitted DOM is byte-identical (`state` is `undefined` so React omits `data-vs-indicator-state`; `orphans` is `[]` so the orphan section renders `null`). Proven behaviourally, not by inspection: `collab-comment-source.test.tsx` mounts `IndicatorLayer path=… mode="markdown"` exactly as `markdown-editor.tsx` does, with a stubbed `/__vs/comments?path=a.md`, and asserts the marker lands on the `[data-vs-loc]` block with the same title and no extra attribute; and mounts the local `CommentPanel` and asserts the `Intro · L10` row. `indicator-layer.tsx` is in the 0.2 suite's `localModules` list, so it also still contains no `documentId`/`nodeVersion`/`querySelector` and exactly one `requestAnimationFrame` — the collab keying lives in `collab-comment-source.ts`, which is why local mode cannot regress through it.
    2. **`use-comments.ts` is untouched — neither widened nor duplicated.** Widening is not available: the 0.2 suite pins that file against `documentId`/`nodeVersion` and against any `/__vs/collab` string, and editing it is out of bounds. A sibling hook would also be premature — 7.2 shipped create/reply/patch but **no collab comment-listing route**, so there is nothing for a hook to read yet. Comments therefore enter the shared components as data (`CommentPanelSource.comments`, `collabIndicatorTargets(doc, comments)`), which is the seam 11.1 fills. R-7.4's "reuse not duplicate" is about the panel and the indicator layer, and both are literally the same components in both modes.
    3. **Three dead-code wirings closed, each with a live test.** (a) `captureTargetText` + `collabNodeVersion` now run in `POST /__vs/collab/:id/comments` via `anchorFields()`, writing `text=` (the R-6.5 trailer key) and `nodeVersion=` alongside `nodeId`. `collab-anchor.test.ts` asserts the saved record carries the fixture's own block text (clamped to `TARGET_TEXT_MAX`) and the **projection** version 4 rather than the serialized node's `version: 1` — deleting the call fails the assertion, not just a grep. Without `nodeVersion` R-6.3 could never fire, so it is captured at the same moment. (b) `resolveCollabAnchor` drives placement through `collabIndicatorTargets`: the outdated test renders the *same* comment over the *same* DOM and only bumps the projected version, and the rendered marker has to flip to `data-vs-indicator-state="stale"`. (c) `data-vs-uncommentable` gates the affordance: `describe()` returns `{uncommentable: <the renderer's own reason string>}`, the panel replaces the compose form with a notice, and `create()` refuses — a comment there would be born orphaned.
    4. **The three states render differently.** `exact` → plain amber marker on the block. `outdated` → same block, same position (R-6.3 changes presentation, never placement), hollow dashed marker, `data-vs-indicator-state="stale"`, aria/title say "block edited since", and the panel row is suffixed `· outdated`. `orphaned` → **no marker at all**; it appears in the panel's document-level `OrphanList` with an `ORPHANED` label and its last-known target text (R-6.4/R-6.5). A comment with no `nodeId` is a document-level discussion (R-5.7), not an orphan, and is neither placed nor listed as one.
    5. **Not done here, deliberately:** manual re-anchoring UI. `reanchorCollabAnchor` (6.1) is the operation, but nothing persists a changed anchor — the PATCH route takes `status`/`result`/`comment` only. Wiring a button to a store that cannot keep the change would be worse than the gap. Left to 11.1 together with the comment-listing route.

- [x] 7.4 Editor port to `JsonDocument` (deps: 2.1, 2.3, est: ~2w)
  - why: `ui/wysiwyg-editor.tsx` keys dirty-detection on Markdown **string equality** (`md === lastSynced.current`, `:136`) — roughly 120 lines of interaction heuristics with no test coverage today. Markdown is a lossy, normalizing projection, which is exactly why that comparison is stable; `getJSON()` is lossless and carries churn, so a naive port produces false-dirty on caret movement.
  - acceptance: R-2.11 — editor driven from structured state, not a Markdown string buffer.
  - verify: new dirty-detection tests written **before** the port; assert no false-dirty on selection change, arrow keys, or clicking a comment pill.
  - landed: `ui/collab-editor.tsx` (new) + `ui/collab-editor.test.tsx` (new, 19 jsdom tests, written before the module existed). Suite 843 → 862 green, `tsc --noEmit` clean, `core/editing/local-mode.regression.test.ts` **unedited and green (70)**. Decisions:
    1. **A second mount, not a port of `ui/wysiwyg-editor.tsx`.** The local editor's buffer *is* a Markdown string (`value` in, `onChange(md)` out) and its stability comes from the projection being lossy — `md === lastSynced.current` cannot see a caret. Changing its buffer type is not a refactor of its dirty detection, it is a replacement of ~120 untested interaction heuristics, against R-10.1's "a local regression is worse than collaboration not shipping". The architecture already drew this line: `import-boundary.test.ts` records that the local editor "imports `markdownToInjectable` and must keep doing so — it is the local editor's load path, explicitly out of scope", and the `ui/(node-id|publish|collab)*` name prefix is how the collaboration path is defined. `wysiwyg-editor.tsx` is untouched; nothing local imports the new module.
    2. **Dirty detection is two-stage.** Stage 1 is Lexical's own `dirtyElements`/`dirtyLeaves` on the update listener: a selection-only update touches no node, so it is discarded without serializing anything — a cost filter, never the verdict (a real edit always marks nodes dirty). Stage 2 is the verdict: `collabDocumentSignature(getJSON())` compared against the baseline. **Ignored: `$` (all NodeState, i.e. `nodeId`) and `direction`.** `$` is dropped by reusing `nodeContentSignature` from 2.2 — the same definition of "content" version bumping uses, so the editor and the store cannot disagree about what an edit is, and a backfill can never read as one. `direction` is derived by the reconciler from the text and can flip `null` → `"ltr"` on a reconcile the user did not cause; the text it is derived from is already compared. **Not ignored:** text, formatting marks, `format`/`indent`/`tag`/`listType`, child structure. Node keys and the selection never appear at all — Lexical serializes neither, which is what makes a lossless `getJSON()` usable here. There are no DOM listeners and no MutationObserver: stage 1 answers what they existed to guess at.
    3. **The deferred `injectJSON` is handled by capturing the baseline from the EDITOR, not from the document that went in.** The load runs `INJECT_SETTLE_MS` (150 ms) before the baseline is read, and until the baseline exists nothing is reported at all — so whatever settling Luthor does is part of the clean state by construction and an inject can never read as dirty. The test samples continuously across the whole window and asserts clean at every sample.
    4. **NEW Luthor finding: `injectJSON` must be issued one macrotask AFTER `onReady`, not inside it.** Luthor runs its own initial-content pass in the same tick as `onReady` and overwrites a state injected during it — the document mounts as a single empty paragraph, silently, with no error. This cost most of the debugging on this task; it refines 0.1's finding 2 (`onReady` is where the ref appears, not where the inject may be issued). Also observed: under jsdom `onReady` does not fire for ~300 ms, so the test harness polls for the loaded document rather than sleeping a fixed amount.
    5. **`markdownSourceOfTruth` is off, proved three ways** — a comment-stripped source scan of the module (with a positive control asserting the flag *is* detected in `wysiwyg-editor.tsx`, so the check is not vacuous), and behaviourally: every block's `nodeId` survives mount → edit → `getJSON()`, while the same document run through `markdownToJSON(jsonToMarkdown(...))` (what Luthor substitutes under the flag) comes back with every id gone.
    6. **Identity is reconciled before the inject.** `injectJSON` runs no node transforms, so a document that arrives id-less would mount id-less; `reconcileDocumentIdentity` runs on the way in (a no-op when ids are already present) and `createNodeIdExtension` covers everything created afterwards. Pinned: a split block keeps its id on the surviving half and the new half gets a fresh unique one.
    7. **`generatePublishPayload` is called from here**, through `handle.publish()`; the reader closure is this editor's own `() => api.current.getJSON()`, so the single read R-12.8 requires is a read of live editor state at the instant of publish. `handle` also carries `readDocument`, `isDirty`, `markClean` (adopt the current state after a successful publish) and the live `LexicalEditor`. Not wired to a host yet — that is 7.5/11.1, matching how 7.1 and 7.3 landed.

- [x] 7.5 Both hosts expose collaboration mode (deps: 7.2, est: ~2h)
  - why: shipping it in the shared UI + route layer is what keeps the two hosts from forking.
  - acceptance: R-7.6 — standalone CLI and Vite plugin both expose it with no host-specific code.
  - verify: smoke both hosts.
  - landed: satisfied structurally by 7.2 (`core/vite/routes/collab-wiring.ts` — neither host recognises a single collaboration sub-path; both call `createCollabWiring` + `createCollabRoutes` and delegate) and by 9.2/9.3/8.2a extending that same shared module. **Verified at runtime**, not only by source assertion, against two real servers — the built standalone CLI (`node dist/cli.js`) and the Vite dev host:
    - `GET /__vs/collab` returns a byte-identical `CollabAvailability` from both: `{"available":false,"reason":"not-configured",…}` (R-7.6, R-7.8, R-9.19).
    - Request guard live on both: `Sec-Fetch-Site: cross-site` → **403**, non-loopback `Host: evil.example` → **403**, absent `Sec-Fetch-Site` → **200** (R-9.13, R-9.14). `POST /__vs/collab/:id/publish` cross-site → **403** before reaching the route layer (R-9.16).
    - Local mode unaffected: `/__vs/tree` and `/__vs/dir` → 200; a rendered markdown file carries **555** `data-vs-loc` and **0** `data-vs-node-id` / `data-vs-indicator-state` / `data-vs-orphan-list` — runtime proof of R-10.6 and of 7.3's claim that local mode takes an identical code path.
    - No collaboration controls surfaced in the UI with no config present; the browser snapshot is identical to the pre-collaboration baseline (R-7.8).
    - `npm run build` succeeds and `npm run check:bundle` passes with all collaboration code landed — `dist/cli.js`, `dist/vite/index.js`, `dist/index.js` carry no `react` / `react-dom` / `@lyfie/luthor` (R-12.6, R-3.3).

## Unit 6: Anchor resolution — Lane A

- [x] 6.1 `nodeId` lookup resolver + orphan handling (deps: 7.1, est: ~2d)
  - why: with identity in the JSON and no diff position, resolution is a direct lookup — about five lines. The value here is in the *degraded* states, which are the ones users actually live in.
  - acceptance: R-6.1 — resolve by `documentId`+`nodeId` only, never line/snippet/heading; R-6.2 — exact anchor on version match; R-6.3 — version mismatch still anchors, flagged outdated; R-6.4 — unresolvable `nodeId` shown as orphaned, never discarded; R-6.5 — orphans appear in the document-level view with last-known target text and manual re-anchoring; R-6.7 — no line/snippet fields required on collaborative comments.
  - verify: table-driven tests per state. R-6.6 — assert `resolveMarkdownAnchors` is untouched and `anchor-resolver.test.ts` stays green.
  - landed: `core/collaboration/anchor-resolution.ts` + `anchor-resolution.test.ts` (22 tests), `ui/collab-anchor-resolver.ts` + `collab-anchor-resolver.test.tsx` (10 tests, jsdom). **Split in two halves, deliberately.** The *state* half is pure JSON and lives in `core/`: `resolveCollabAnchor(ref, doc): CollabAnchorResolution` returns a discriminated `state: 'exact' | 'outdated' | 'orphaned'`, testable with no jsdom and usable from routes/jobs. It reuses 3.1's `resolveNodeIn` — **there is no second resolver** — and is roughly five lines of lookup; everything else in the module is the degraded states. The *DOM* half is one function in `ui/collab-anchor-resolver.ts` (`findCollabBlock` / `resolveCollabAnchorElement`), because only it needs `document`; it locates via 7.1's `collabBlockSelector` and refuses a block marked `data-vs-uncommentable`. **Versions come from the `nodes` projection (task 2.2), never from the serialized node's own `version`** — that is Lexical's node-class schema version and would flag every comment outdated (a test pins the two apart: tree `version: 1`, projection `version: 3`). A mismatch needs *two* known versions; one unknown is `exact`, not a fabricated `outdated`. A `documentId` that is not this document's is an orphan, not a lookup to retry. **R-6.5 — last-known target text is carried on the comment's trailer under the new well-known key `text` (`COLLAB_TARGET_TEXT_KEY`), captured at creation time by `captureTargetText(doc, nodeId)` and clamped to 160 chars** (local mode's `snippet` budget): once the node is gone the `nodes` projection entry went with it, and the trailer is the only part of a collaborative comment that survives a GitHub round-trip. That is a protocol *convention*, not a format or type change — `CommentTrailer`'s index signature already carries unknown keys through 5.1's parse/format exactly as `resolved=` (5.2) and `key=` (5.3) do, so `comment-projection.ts` needed no edit and got none. **Manual re-anchoring is a real operation, not a UI stub:** `reanchorCollabAnchor(anchor, doc, nodeId): CollaborationAnchor | null` returns the anchor moved onto the chosen block at that block's current projected version, preserving the GitHub binding and unknown fields (R-1.8), and returns `null` rather than a half-built anchor when the target does not resolve *or* the projection has no version for it — re-anchoring to a block the renderer will not version-stamp would produce a comment that is silently `exact` forever. `orphanedAnchors(refs, doc)` is what the document-level view (7.3) lists. R-6.1/R-6.7 are asserted three ways: behaviourally (a ref stuffed with `startLine`/`endLine`/`snippet`/`endSnippet`/`heading` resolves identically to one without them, and a perfect snippet does not rescue a dead `nodeId`), at the type level (a complete `CollaborationAnchor` is `nodeId` + `nodeVersion` + `github`; `tsc --noEmit` is the assertion), and against the module's own comment-stripped source, which names none of the five fields. **R-6.6 — no edit to local mode:** `ui/anchor-resolver.ts` and `ui/markdown-surface.tsx` are untouched (`git status` shows only new files), nothing here imports them, and the jsdom suite re-asserts `resolveMarkdownAnchors`'s line / heading-fallback / range-extension behaviour independently so a regression fails here too, not only in `anchor-resolver.test.ts` (9 tests, green). The two surfaces are provably disjoint: the collaboration DOM emits no `data-vs-loc`, the local surface carries no node identity, and each resolver finds nothing in the other's DOM. Suite 639 → 671.

## Unit 8: Jobs, sync, publish — Lane C

- [x] 8.1 Per-document job hub registry (deps: 3.1, est: ~1w)
  - why: `createApplyHub` (`core/vite/routes/apply.ts:251`) is a module-level singleton — `let events`, `let running`, one `subs` Set — so one run at a time, globally. Collaboration needs concurrent per-document jobs. This is new code, not reuse.
  - acceptance: R-8.1 — registry keyed by `documentId`, no shared state; R-8.2 — SSE fan-out at `GET /__vs/collab/:id/events`; R-8.3 — jobs for create, commit, sync, re-resolve, publish; R-8.4 — late subscriber can recover current state.
  - verify: two documents running jobs concurrently without interference.
  - landed: `core/collaboration/job-hub.ts` + `job-hub.test.ts` (26 tests). `createJobHubRegistry({ maxEvents?, now? })` → `hub(documentId)` lazily creates a `DocumentJobHub` owning its own `state` / `job` / `events` / `subs` — nothing module-level, so two documents run jobs at the same instant with disjoint logs and subscribers (the isolation test asserts no `jobId` from one document appears in the other's frames). Job *bodies* are injected (`start({ kind, run, idempotencyKey? })` where `run: (ctx: JobContext) => Promise<void>`), so 8.2 / 8.3 supply real GitHub work without reshaping the hub; the seven `JobKind`s of R-8.3 are declared here. `LifecycleState` (the LLD §7 diagram) is declared here too and *held*, not enforced — the Ready gate and merge re-verification stay in 8.4; the only transition the hub owns is `failed` on an uncaught rejection. Decisions: a second job for a busy document is **rejected 409, never queued** (a queued publish could land after a sync that invalidated it, and the caller cannot see what it is behind); the event log is bounded at **500 frames**, dropping oldest and counting them in `droppedEvents`, which does not weaken R-8.4 because `state` / `running` / `job` are tracked outside the log; `dispose(documentId)` / `disposeAll()` abort the running job, end subscribers and drop the map entry. Bodies are invoked synchronously (as `runApply` is) so a start-then-cancel in one turn still reaches an abort listener. `SseSink` is a structural subset of `ServerResponse`, so 7.2 mounts `GET /__vs/collab/:id/events` → `hub(id).subscribe(res)` and `GET /__vs/collab/:id` → `hub(id).status()` with no HTTP in the unit tests. Suite 277 → 303.

- [x] 8.2 Document creation + sync (deps: 4.1, 8.1, est: ~3d)
  - why: sync must run through one entrypoint so a webhook receiver could later drive it unchanged.
  - acceptance: R-8.5 — `start` creates branch, commits JSON, opens PR, returns `pullNumber`+`headSha`; R-8.6 — interval polling and explicit sync; R-8.7 — both through one entrypoint; R-8.8 — no webhook receiver required.
  - verify: fixture-replayed; assert both sync paths call the same function.
  - landed: `core/collaboration/lifecycle.ts` + `lifecycle.test.ts` (21 tests). `createLifecycle({ adapter, repo, store, hubs, onSync?, syncIntervalMs?, scheduler? })` supplies **job bodies** to the 8.1 hub — it owns no event log and no state of its own, and every GitHub call goes through the 4.1 adapter (Contents API, never git), every comment read through the 5.1 projection. **How `pullNumber`+`headSha` surface (R-8.5):** a `JobBody` returns `void`, so `start` persists a `GitHubBinding` onto the stored document via the 3.1 `DocumentStore` and `readGitHubBinding(store, documentId)` reads it back — the store is the only *durable* channel, so a client that was not subscribed when the PR opened (or reconnects after a restart) still finds it; the SSE stream carries the same facts as a log line plus the `state: 'pr-open'` transition. **R-8.7:** `lifecycle.sync(documentId, trigger)` is the single entrypoint — `'user'`, `'poll'` and `'webhook'` run identical code, the trigger is reporting only. The poller dereferences `lifecycle.sync` at tick time (not a captured private copy), so a `vi.spyOn(lifecycle, 'sync')` in the test sees both the polled and the user-initiated call and asserts both produce the same endpoint and the same projected result. **Poller:** owned by this module, injected `IntervalScheduler` (no real timer anywhere in the suite; the default `unref`s so polling never holds the CLI open), 30s default, started/stopped by the host (7.2) via `startPolling` / `stopPolling` / `stopAllPolling`; overlap is handled by the hub's existing 409 — a tick that fires while the previous sync is in flight is **dropped, not queued**. **Sync writes nothing to the document** — not `doc`, `nodes`, `frontmatter`, `title` or the binding — it reads comments and hands a `SyncResult` to `onSync` (where 5.3 plugs the cache in), so a poll cannot clobber an in-flight edit. **Partial create failure (leaves R-8.18 tractable for 8.4):** the binding is written twice — once right after the branch exists carrying `branch` with **no `pullNumber`**, once after the PR opens carrying both — so a commit/PR failure leaves the durable signal "branch created, nothing opened against it"; 8.4 detects `branch && pullNumber === undefined`, deletes `refs/heads/<branch>` and clears it. `branchNameFor(documentId)` is deterministic (`visual-spec/<id>`) so cleanup does not depend on that write landing. **R-11.1:** `buildPullRequestBody()` renders a human table (repo / branch / document) plus a fenced `npx @metuur/visual-spec collab open --repo <owner>/<repo> --branch <branch> --document <id>` command, and ends on a 5.1 trailer (`<!-- visual-spec: branch=… documentId=… documentPath=… owner=… repo=… -->`) so task 11.1 reads it back with `parseCommentBody` rather than a second format. Suite 342 → 363; `tsc --noEmit` clean.

- [x] 8.2a Wire the 8.2 job bodies into the 7.2 routes in both hosts (deps: 7.2, 8.2, est: ~3h)
  - why: 7.2 and 8.2 were built concurrently and landed with **incompatible seams** — both wanted to own `hub.start()`. 7.2's routes injected job bodies as factories `(input) => JobBody` and started the hub themselves; 8.2's `createLifecycle` built the bodies privately and called `hubs.hub(id).start()` itself, exposing only `start()` / `sync()`. So the shipped `/__vs/collab` routes ran 7.2's **throwing stubs** and 8.2's real GitHub work had no HTTP entrypoint at all. Both suites passed in isolation, which is exactly why it slipped through: neither one drives both layers.
  - acceptance: R-7.1 — the route family actually performs the work it advertises; R-7.6 — the wiring is identical in both hosts, no host-specific collaboration logic; R-8.6 — the interval poller is started and stopped by the host; R-9.19 — nothing GitHub-touching is constructed when collaboration is unconfigured.
  - verify: an **integration** test that drives HTTP-shaped requests through `createCollabRoutes` with the real lifecycle bodies and an injected `GhExecutor`, asserting on the `gh api` endpoints reached; 8.2's 21 tests unchanged.
  - landed: `core/collaboration/lifecycle.ts` (refactor, no public-API change) + new `core/vite/routes/collab-wiring.ts` + `collab-wiring.test.ts` (7 tests). The `create` / `sync` bodies are extracted out of the `createLifecycle` closure into `createLifecycleBodies({ adapter, onSync? })`, whose `CreateBodyInput` / `SyncBodyInput` are deliberate **subsets** of 7.2's `CreateJobInput` / `SyncJobInput` (`repo` and `store` now arrive per call instead of per instance), so the factories drop straight into `CollabDeps.bodies` and the route hands its own input through untouched. `createLifecycle` keeps its exact signature and its `start` / `sync` / `startPolling` / `stopPolling` / `stopAllPolling` / `pollingDocumentIds` surface — those two methods are now thin wrappers that bind this instance's repo + store and hand the body to the hub, so the poller and the HTTP route provably run the *same* code. 8.2's `lifecycle.test.ts` (21 tests) was **not touched**. `createCollabWiring({ config, documents, jobs, onSync?, exec?, scheduler? })` is the one place either host builds anything: it returns `{ bodies, stopAllPolling, pollingDocumentIds }`, and both `src/server.ts` and `core/vite/md-plugin.ts` now call it and pass `bodies: collabWiring.bodies` to `createCollabRoutes`, then `collabWiring.stopAllPolling()` alongside `collab.dispose()` on `close`. **R-9.19:** when `resolveConfig()` yields `collaboration: null` the wiring returns before `createGitHubAdapter` is ever called — no adapter, no lifecycle, no poller, `bodies: {}` — and the test asserts the injected executor is never invoked and `Object.keys(bodies)` is empty; local mode is untouched. **Polling:** `create` is wrapped so `startPolling(documentId)` fires the moment the document is PR-open (the only moment this package can observe that transition today); the poller re-reads the host's `documents()` thunk per call so a runtime re-root is honoured. `open` (R-11.2) and `publish` (R-8.9…) keep 7.2's honest `notImplemented` stub — 8.3 must export a factory `(input: PublishJobInput) => JobBody` and add it to the spread in `collab-wiring.ts`; 11.x the same for `(input: OpenJobInput) => JobBody`, plus a `startPolling` call once it has attached a PR. `onSync` is threaded through the wiring unchanged, so task 5.3's comment cache is a one-line wire-up in both hosts. Suite 487 → 494; `tsc --noEmit` clean.

- [x] 8.3 Publish: commit then verify, no merge (deps: 2.3, 8.1, est: ~4d)
  - why: ordering is load-bearing. Verifying *after* merge would discover bad bytes only once they are in the base branch, in a state that by rule must not self-heal. Merge is deliberately not part of publish — that removes the irreversible half of the write primitive from the localhost endpoint.
  - acceptance: R-8.9 — payload of `json`+`markdown` required, incomplete rejected; R-8.10 — commit, verify committed blob against payload bytes, **do not merge**; R-8.11 — expected hash computed server-side, not trusted from the client; R-8.12 — Markdown treated as opaque; R-8.13 — committed Markdown readable from the branch for the agent's summary; R-8.14 — publish completes if the client disconnects after the payload is accepted.
  - verify: R-12.7 — publish rejects a payload missing `json` or `markdown`; R-12.4 — publishing a document with no unsupported nodes reproduces byte-identical Markdown for the same input; assert no merge call is made.
  - landed: new `core/collaboration/publish.ts` + `publish.test.ts` (32 tests), plus one integration test appended to `core/vite/routes/collab-wiring.test.ts`. `createPublishBody({ adapter })` returns the factory `(input: PublishBodyInput) => JobBody` — a **sibling** of `lifecycle.ts`, not a change to it — whose `PublishBodyInput` is a deliberate subset of 7.2's `PublishJobInput`, so `collab-wiring.ts` wires it with one line (`publish: createPublishBody({ adapter })`) and 7.2's throwing stub is gone. **What gets committed:** two artifacts, both derived from `document.documentPath` so nothing has to be stored to find them again — the canonical JSON envelope at `documents/<id>.json` with the client payload installed as `doc`, and the generated Markdown at `markdownPathFor(documentPath)` (`.json` → `.md`, i.e. `documents/<id>.md`). `markdownPathFor` is exported and pure, so 11.x and the changelog derive the path rather than reading a persisted field that could drift. **Verification (R-8.10 / R-8.11):** after both commits each path is **read back off the branch** with `getFile` and checked twice — content must equal the bytes sent, and the blob sha GitHub reports must equal `gitBlobSha(bytes)` computed here with `node:crypto` (`sha1("blob " + byteLength + "\0" + content)`, cross-checked in the suite against `git hash-object` for the empty blob `e69de29…`, `hello world\n` → `3b18e512…`, and a multi-byte case proving the header counts **bytes** not UTF-16 units). Comparing against the `contentSha` the `PUT` echoed is one fewer round trip and was rejected: it is GitHub's echo of the write it just performed and says nothing about the branch afterwards — a test proves verification still passes when that echo is a bogus value, so correctness never depends on it. What the read-back does **not** catch: a push landing after verification (it is a snapshot, not a lock), non-UTF-8 byte sequences (the sha comparison covers most of that gap), and base divergence. **R-8.12:** `markdown` is committed verbatim — five deliberately malformed inputs (unclosed fence, ragged table, unbalanced HTML, CRLF + mixed markers, NUL/zero-width/trailing space) publish byte-identical, and CRLF survives because the Contents API is the only write path (never `git add`). **R-8.14:** this body **never reads `ctx.signal`** — unlike create/sync it has no `throwIfAborted`, so a subscriber closing cannot cancel it; proved through the real hub (only subscriber disconnects mid-commit, job still reaches `published` and the bytes land) and directly with an already-aborted signal. **R-8.22 room:** nothing here reads `repo.baseBranch` (asserted), the read-before-write already fetches each target's current blob — the exact input a base comparison needs — and failures are a typed `PublishVerificationError` so 8.4 adds a sibling `PublishBaseDivergedError` thrown *before* the commit without touching this module. States: `publishing` → `verifying` → `published`; a mismatch throws and the hub records `failed`, with the test asserting still exactly two PUTs (R-8.21 — no regeneration, no overwrite). **No merge, asserted three ways:** no `/merge` endpoint and no `/pulls/` call in the recorded argv, `vi.spyOn(adapter, 'mergePullRequest')` never called, and the state sequence never contains `merged`. **No git subprocess:** every recorded call's `args[0]` is `api`. R-8.9/R-12.7's 400 stays route-level (`collab.test.ts`); the body adds its own guard for direct callers and rejects before any GitHub call. Suite 557 → 590; `tsc --noEmit` clean.

- [x] 8.4 Failure states + idempotency (deps: 8.3, est: ~3d)
  - why: R-8.24's "leave it reconcilable" is a wish, not a specification. A double-clicked publish must not produce two branches and two PRs.
  - acceptance: R-8.15 — Ready gate; R-8.16 — new comment returns a Ready document to PR-open; R-8.17 — merge-time re-verification (closes the poll-window TOCTOU); R-8.18 — no orphaned branch on partial create; R-8.19 — externally closed PR reaches a closed state; R-8.20 — base-branch conflict reported, no auto-resolution; R-8.21 — verification failure aborts before merging; R-8.22 — base divergence is a **distinct** state from verification failure; R-8.23 — idempotency tokens; R-8.24 — failures reported over SSE.
  - verify: inject failure at each step and assert the recorded state and that retry does not duplicate.
  - landed: new `core/collaboration/failure-states.ts` + `failure-states.test.ts` (68 tests), plus additive edits to `job-hub.ts` (8.1), `github-adapter.ts` (4.1) and `collab-wiring.ts` (8.2a). **`lifecycle.ts` and `publish.ts` are not modified at all** — the two behaviours that had to reach inside them are applied as wrappers (`withOrphanCleanup`, `withPublishFailureStates`), so both suites describe exactly what their own bodies do and neither needed an edit. **Ready is derived, never stored (LLD §7):** `deriveReadiness(comments)` and `deriveLifecycleState({ pull, readiness })` are pure, there is no `ready` field on the document, the binding or the hub, and R-8.16 therefore needs no rule — the same function that derived `ready` derives `pr-open` the moment an unresolved comment exists. **R-8.17 (TOCTOU):** `merge` re-reads the conversation through the 5.1 projection *itself*, immediately before merging, and ignores both the hub's state and the last poll; the test injects a comment through an `afterListComments` hook that fires the instant the readiness read returns, then asserts `state === 'pr-open'` and that no `/merge` endpoint was reached. **Four failure states, not one:** `LifecycleState` gained `conflicted` (R-8.20), `verification-failed` (R-8.21) and `base-diverged` (R-8.22) beside `failed`; they travel as ordinary `{ type: 'state' }` SSE frames so a client discriminates on a field, never by parsing a message, and the paired test asserts the two publish failures produce different `job-done` states from identical inputs. The hub keeps a state a body declared before rejecting — opt-in per error via `markStateDecided` / `isStateDecided`, because whether a declared state survives is a property of *why* the body threw, not of the state; a refusal (`markReady`, `merge`) is not a `failed` job, it is a correct job whose answer is no. **R-8.23 idempotency:** a `Map<key, JobRecord>` in `DocumentJobHub.start`, consulted **before** the 409 branch — a retry of a still-running job gets the original `jobId` with `deduplicated: true`, not a 409; a retry after success re-runs nothing; a retry after **failure releases the key**, because LLD §7 has an explicit `Failed → PROpen : retry` edge and nothing landed to duplicate. Bounded at 64 keys per document, oldest evicted, cleared by `dispose()` — no TTL, because the clock is injected and a wall-clock expiry would make the same retry behave differently depending on how long the laptop slept. **R-8.18:** `cleanupOrphanedBranch` detects 8.2's durable signal (`branch && pullNumber === undefined`), deletes `refs/heads/<branch>` and clears the binding while keeping the draft; it falls back to the deterministic `branchNameFor` so a create that died before the binding write is still cleaned, and deleting an absent ref is success, which is what makes it safe on every retry. **R-8.22:** `detectBaseDivergence` uses `compareCommits(repo, branch, baseBranch)` — its `files` are exactly "what changed on base since the branch point". The rejected alternative (comparing each path's blob on base against the branch) cannot work: publish's own commits make them differ by design, so every second publish would false-positive. **Adapter additions (4.1, additive):** `deleteBranch` (404/422 ⇒ `false`), `getPullRequest` → `PullRequestStatus` with `merged` / `mergeable` (`null` kept as a third answer, not a conflict), `compareCommits` → `BranchComparison`. **R-8.24:** `reconcile` is the single "what state is this actually in?" body and the recovery path from every row of the failure matrix; the matrix asserts, per step, the recorded state, a `job-error` frame, a `job-done` carrying the same state, the forbidden endpoints not reached, and the state a later sync derives. **Not wired to a route:** `markReady` / `merge` / `reconcile` are exported bodies with no HTTP entrypoint — mark-ready and merge are author-only (R-9.9, task 9.2) and merge needs the R-9.17 terminal confirmation (task 9.3). Wiring any of them, or turning on publish's opt-in `checkBaseDivergence`, changes the shipped `gh api` call sequence and would require editing `collab-wiring.test.ts`'s exact-endpoint assertions — a contract change left to whoever owns those routes. Suite 619 → 687; `tsc --noEmit` clean.

## Unit 9: Authorization — Lane B

- [x] 9.1 Shared request guard for both hosts (est: ~4h)
  - why: `/__vs` had no origin checking and `readJsonBody` never inspected `content-type`, so a cross-origin `text/plain` POST was a CORS simple request — putting `comments/add` and `apply/start` in reach of any page in the user's browser, and the apply run splices comment text into a prompt for an agent running with `acceptEdits`. Publish would escalate that to a remote write with the author's credential.
  - acceptance: R-9.13 — `Sec-Fetch-Site` cross-site/same-site rejected and non-loopback `Host` rejected on every `/__vs` route in both hosts; R-9.14 — absent `Sec-Fetch-Site` allowed (non-browser clients carry no ambient authority); R-9.15 — one shared implementation, registered ahead of every handler.
  - verify: 11 unit tests in `core/vite/request-guard.test.ts`; full suite green (153).
  - landed: _uncommitted_ — `core/vite/request-guard.ts`, `core/vite/request-guard.test.ts`, `src/server.ts:176`, `core/vite/md-plugin.ts:147`

- [x] 9.2 Role classification + author-only enforcement (deps: 4.2, 7.2, est: ~3d)
  - why: gating on "PAT owner == PR author" would lock reviewers out entirely. The valuable property is that GitHub itself enforces the boundary — a reviewer's credential cannot push — so single-writer holds even if a server check is wrong.
  - acceptance: R-9.5 — identity resolved from the credential; R-9.6 — comments attributed to the acting user's own account, identity never encoded in bodies; R-9.7 — author vs reviewer classification; R-9.8 — reviewer can read/comment/reply with no write access; R-9.9 — edit/commit/mark-ready/publish/merge author-only server-side; R-9.10 — reviewer request rejected server-side even when the UI control is hidden; R-9.11 — UI hiding never satisfies an authz requirement.
  - verify: reviewer-token fixture attempting each author-only operation.
  - landed: `core/collaboration/authorization.ts` (`createCollabAuthorizer`, `OPERATION_POLICY`, `CollabRole`) + `authorization.test.ts` (34 tests) + 4 recorded fixtures (`repo-write-access.json`, `repo-read-only.json`, `pull-author-octocat.json`, `pull-author-other.json`), wired through `collab-wiring.ts` (`CollabWiring.authorize`) and one line in each host (`authorize: collabWiring.authorize`). No route was edited — 7.2's `authorize` seam took the whole change, as its author predicted. **`OPERATION_POLICY` is a total `Record<CollabOperation, 'any-role' | 'author-only'>`**, so a newly added operation is a type error rather than a silent default-to-permitted; `create` and `publish` are the two `author-only` entries (the only ones in this family that commit), and `read` / `sync` / `open` / `comment` / `reply` / `edit-comment` are `any-role` — `sync` because a reviewer who cannot refresh cannot see the comments they are here to read, `open` because it is the Unit 11 reviewer-onboarding path and writes nothing to GitHub. **Role resolution costs zero `gh` calls for an `any-role` op** (the policy table is consulted first, so a GitHub outage never blocks reading or commenting) and at most two for an author-only one: `GET /repos/{owner}/{repo}` → `permissions.{admin,maintain,push}` for write access (chosen over `/collaborators/{user}/permission`, which itself requires push access and so cannot tell "no write access" from "cannot ask"), then `GET /repos/{owner}/{repo}/pulls/{n}` → `user.login` for the PR author. Permission is cached per `owner/repo` under a 60s TTL, which bounds how long a revoked write grant can still read as `author`; the PR author is cached per `owner/repo#number` with no expiry because GitHub does not let a PR change author; **failures are never cached**, in either direction. **Fail closed:** `gh` unavailable, repo or PR not found, a payload with no `permissions`, a non-JSON body, or a throwing document store all refuse an author-only operation with **403** and `"… the collaboration role could not be determined (…)"` — deliberately not 503, which is this family's "collaboration unavailable" answer and carries a `CollabAvailability` payload the UI acts on. Absent a PR binding, write access alone decides: `create` is the operation that opens the PR, and whoever opens it becomes its author. R-9.6 is verified against 5.1's shipped code rather than rebuilt — the comment is posted through the real `githubCommentStore`, and the test asserts the `gh api` request body has exactly one key (`body`), that the body contains no login and no `login=`/`author=`/`user=` trailer field, and that the attributed user read back is GitHub's (`reviewer-rita` from the recorded response), not the configured login. Suite 619 → 653.

- [x] 9.3 Publish gating + cross-origin readability test (deps: 8.3, 9.1, est: ~1d)
  - why: publish accepts client bytes and commits them, so the guard is a precondition for exposing it — not later hardening. And Vite's dev server has historically applied permissive CORS defaults; if those reach `/__vs`, a blind write primitive becomes read-plus-write.
  - acceptance: R-9.16 — publish route not exposed until the guard is in place; R-9.17 — merge (if offered in-app) requires an out-of-band terminal confirmation; R-9.18 — `/__vs` responses not readable cross-origin in **either** host.
  - verify: cross-origin fetch test against both hosts; assert `server.allowedHosts` is not `true`/`all`.
  - landed: `core/vite/guard-attestation.ts` (new) + `core/vite/cross-origin.test.ts` (new, 21 tests); two additive lines in each host. **R-9.16 made structural, not incidental:** 9.1's source-position assertion says the guard sits *above* the dispatch, which is invisible at runtime and expressed differently in each host — a Connect middleware in one, an `if` in the other — so deleting either one silently opens publish. The guard now stamps the request it cleared (`attestGuardRan(req.headers)`, a `WeakSet` keyed on the per-request headers object — nothing a client sends can forge it, nothing outlives the request), and both hosts refuse the `/__vs/collab` dispatch outright with `GUARD_NOT_RUN` when they cannot confirm it: **removing the guard turns collaboration off rather than turning publish on**. The test asserts `attestGuardRan(` appears exactly once per host, after `checkRequest`, and that `guardRan(req.headers)` precedes `collab.handle(`. **R-9.17 — decision: no in-app merge route exists.** Publish deliberately stops at commit-then-verify (8.3); merge is the one irreversible remote write, so the cheapest way to satisfy "out-of-band confirmation shown only on the terminal" is to keep the operation off the HTTP surface entirely and let the author merge in GitHub's UI under GitHub's own auth. No confirmation value is generated because the conditional is never entered — asserted, not left silent: `POST /__vs/collab/:id/merge` is 404 from loopback in both hosts, and no host/route/wiring/publish/lifecycle module mentions `mergePullRequest` or a `merge` path. `adapter.mergePullRequest` stays available and unreferenced. **R-9.18 — Vite's CORS defaults were measured, not assumed.** They do **not** reach `/__vs`, and not by luck of configuration: this package's middlewares are registered in `configureServer`, which runs *before* Vite installs its internal CORS middleware, so `/__vs` terminates the request before any `Access-Control-*` header can be attached. Proved at the worst case — the test's Vite host resolves `cors: true` **and** `allowedHosts: true`, and `/@vite/client` does get `Access-Control-Allow-Origin: *` under that config while `/__vs` gets none. No live vulnerability. `allowedHosts` is safe today besides: unset in `vite.config.ts` (Vite 6 resolves it to `[]`), and the guard's non-loopback `Host` rejection covers the rebinding case independently. The cross-origin test starts **both hosts for real** and speaks raw `node:http` (not `fetch`, because `Host`/`Origin` are forbidden header names) with the exact set a browser attaches: `Origin: https://evil.example`, `Sec-Fetch-Site: cross-site`, `Sec-Fetch-Mode: cors` over a loopback `Host` (plain CSRF), plus a `same-origin` variant over `Host: evil.example` (DNS rebinding). Both are 403 on `GET /__vs/dir` and on `POST /__vs/collab/:id/publish` — and the refusal is not the route layer's `missing json`/`not-configured`, so the request provably never got that far. Non-readability is asserted twice over: no `Access-Control-Allow-Origin`/`-Credentials` on the 403 **or** on a legitimate 200, and the publish preflight is unanswered. Suite 619 → 640; `tsc --noEmit` clean.

## Unit 11: Reviewer onboarding — Lane A

- [x] 11.1 Open-from-PR path (deps: 7.1, 7.2, est: ~1w)
  - why: the branch artifact is JSON, so a reviewer who only opens the PR sees a payload. Getting them from a PR link to rendered prose is not a convenience — it is the only way the feature works.
  - acceptance: R-11.1 — PR body carries repo/branch/document and the command to open it; R-11.2 — opening by PR reference fetches canonical JSON and renders it with no prior local copy; R-11.3 — works with read-only credentials; R-11.4 — missing read access reported specifically; R-11.5 — the reviewer's own GitHub identity shown so attribution is never a surprise.
  - verify: SC-4 — two machines, two credentials, one read-only; the read-only participant completes open → read → comment.
  - landed: `core/collaboration/open.ts` + `open.test.ts` (18 tests), `core/vite/routes/collab-open.test.ts` (4, the SC-4 simulation), `ui/collab-open-panel.tsx` + `.test.tsx` (6), two read-only adapter methods, and the `open` body in `collab-wiring.ts`. **`createOpenBody({ adapter })` → `(input: OpenBodyInput) => JobBody`**, the shape 7.2's `CollabJobBodies.open` declares; the wiring calls it then `lifecycle.startPolling(input.documentId)`, mirroring `create`, and `stopAllPolling()` on shutdown is what stops it (a per-document stop still belongs to the close path). **R-11.1 is read back, not re-specified:** `readPullRequestReference(pr, fallback)` parses 8.2's `buildPullRequestBody` output with `parseCommentBody` — the same 5.1 parser — and the round trip is asserted against the writer itself, so the human table on github.com can be reworded freely; `pr.headBranch` is the fallback when the trailer names no branch. **R-11.2:** `getPullRequest` → trailer → `getFile(documentPath, branch)` → `store.write`, so the document a reviewer with **no local copy** reads at `GET /__vs/collab/:id` is the one fetched off the branch. **Already-here rule:** the branch is the system of record, so open **refreshes** a stale local copy; the one refusal is a copy already bound to a *different* pull request (`already_attached`, naming both numbers) — silently re-pointing it would move a document out from under an in-flight publish. **R-11.3:** open is two GETs and one local write; `commitFile` / `createBranch` / `createPullRequest` / `mergePullRequest` are unreachable from it, and the SC-4 test drives open → read → comment against a `gh` that 403s every push-requiring endpoint, asserting the only non-GET reached is `POST /issues/42/comments`. **R-11.4** is a typed `OpenDocumentError` with one message per cause — `no_access` (404 vs 403 worded separately), `no_credential` (401), `executor_unavailable` (R-4.10), `not_collaboration_document`, `document_id_mismatch` (names the id the PR actually carries), `document_missing`, `already_attached`, `open_failed`. **R-11.5:** `ui/collab-open-panel.tsx` states "Signed in as <login> — comments you leave here are posted to <owner>/<repo> as <login>" from `GET /__vs/collab` *before* any comment is written, and shows the server's own failure text verbatim. **Entry points, both built:** the panel (PR URL / `#42` / `42` + document id) and `visual-spec collab open --repo o/r --branch b --document d [--pull n]` — the command 8.2 already prints, now real; it resolves the branch to its open PR (`findOpenPullRequestForBranch`, a GET) and posts to its own `/__vs/collab/open`, so there is one open path rather than two. New adapter methods `getPullRequest` and `findOpenPullRequestForBranch` (both reads) plus `PullRequestDetail` (`body`, `headBranch`, `baseBranch`). The panel was **not mounted** at the time of this task — there was no collaboration shell in `App.tsx`; superseded by `3fd0095` (U-1), which mounts it behind a `collabOpen` route. Two stale assertions in `collab-wiring.test.ts` that pinned `open` as stubbed were updated. **The SC-4 simulation is one process:** it establishes that the sequence completes with an empty local store and touches no endpoint a read-only credential is refused; it establishes nothing about a second machine, real `gh`, GitHub's own permission enforcement, or the author observing the comment. Suite 639 → 666; `tsc --noEmit` clean.

---

## Notes

### Landed — verified against the source, not the task text

A post-completion review checked every claim against the code. All seven defects it found are now closed; one line each for traceability.

| Defect | Fix |
|---|---|
| U-1 — collaboration UI not mounted | `3fd0095` — `App.tsx` gains a `collabOpen` route entered from the sidebar footer; `ui/collab-app.tsx` renders `CollabOpenPanel` then a read-only document pane. **Reviewer half only** — the author half is O-1. |
| U-2 — `authorize` fail-open at the host seam | `83489d1` — required on `CollabDeps`, `ALLOW_ALL` deleted, so the seam is a compile error. |
| U-3 — "ready to publish" handback did not exist | `d1888fa` — the collab branch of `apply-prompt.ts` emits `READY TO PUBLISH: <documentPath>` and stops. |
| U-4 — per-document polling had no close path | `3ecd97f` — polling follows SSE subscriber counts; `dispose` idempotent, `stopAllPolling()` remains the backstop. |
| U-6 — preflight cache poisoned on failure | `ff1cfc6` — 60s TTL and delete-on-failure. **Credential half deliberately open**: the key is `${owner}/${repo}#${baseBranch}`, so a `gh auth switch` still resolves the first login for up to the TTL. |
| rate-limit 403 misread as permissions 403 | `117e00b` — `classifyOpenFailure` inspects `x-ratelimit-remaining` and abuse-detection wording before the permissions branch. |
| O-3 — a network error wore a preflight verdict | `04910fb` — the panel's catch labelled a client-side `fetch` throw `preflight_failed`. Reading the render path, `reason` is never displayed; `message` is, so a bare "Failed to fetch" was landing where `gh` remediation text goes. Message fixed, reason corrected though unread. |
| O-4 — unbounded pagination | `04910fb` — capped at 100 pages, and it throws rather than returning a truncated list, since resolve/reply read a missing comment as deleted. Mutation-checked. |
| O-5 — boundary regex missed subdirectories | `04910fb` — `core/collaboration/.+\.tsx?`; a module parked in a subdirectory no longer leaves the guarded set. |
| frontmatter never reached the artifact | `06df41f` — `serializeFrontmatter` emits a YAML block; C0/C1 escaping, reserved-word key quoting, exponent-form coercion. Node and frontmatter losses merged onto one `losses` channel. |
| O-1 — the author half was unmounted | `2060bb1` mounts the real `CommentPanel` in the reviewer view; `6d54ec6` mounts `CollabEditor` in `ui/collab-app.tsx` behind a review/edit mode toggle, with a staged publish that surfaces `losses` in a confirm dialog. **The commit subject names only the guard change** — the mount is the bulk of that diff. `generatePublishPayload` and the frontmatter emitter now have a live caller. |
| O-7 — two built modules had no callers | `8b75468` wires `createRecoveryBodies` to the recovery routes; `527ea9d` wires `deleteCommentCache` / `mergeAndDropCommentCache` to merge (R-5.8). Both were dead-by-dependency; the dependencies now exist. |

U-5 (`droppedNodes` computed and discarded) was **withdrawn**: dropping is the specified outcome (SC-8, "publishes, but not silently") and `prepareDocumentForBridge` substitutes a visible placeholder. The producer was finished ahead of its consumer; the consumer arrived with O-1, as the publish confirm dialog that lists the losses before the author commits.

### Open — re-verified 2026-08-07 after the mount work

Four items. None of them is code that exists and doesn't run — that class is now empty, and `ui/reachability.test.ts` fails if it comes back. What is left is one design decision, one thing only a real repository can prove, and two gaps in the written product.

- **O-2 — the JSON diff is not collapsed, and this is not a one-liner.** Marking the document path `linguist-generated=true -diff` is what lands a reviewer on the instructions rather than a wall of JSON. But `.gitattributes` has to exist in the **user's** repository, not this one, and `documentPath` is an arbitrary per-document string — so adding a file here does nothing. The real options are (a) publish/create commits or amends a `.gitattributes` in the target repo, which needs don't-clobber and merge-conflict semantics, or (b) it becomes a documented manual step folded into O-9's onboarding. Decide which before estimating. Previously mis-sized as "one line".
- **O-6 — the two-credential path has never run.** `authorization.test.ts` is 34 cases of fixture-swapped permission responses. Single-writer is enforced *by GitHub permissions rather than by convention*, so that enforcement is currently an assertion in a fixture. One recorded manual run against a real repo, with the real 403 body fed back into `repo-read-only.json`, closes it.
- **O-8 — the drive-by reviewer is unspecified.** 9.2 classifies from write access; on a public repo a commenter with no repo access can still post issue comments. Either require collaborator-with-read and say so in onboarding, or add a third role bucket. Today it resolves by accident.
- **O-9 — author first-run onboarding is assumed.** Every story starts from "the author already has a repo, a branch, and write credentials." Reviewer onboarding got a unit because someone flagged it; this got nothing because it was invisible.

### Corrections — claims from the previous revision that did not survive checking

Recorded so they are not re-litigated.

- **"Luthor is unpinned, and that risk is live now" — false.** `packages/visual-spec/package.json:58` is `"@lyfie/luthor": "2.9.0"` — exact, no caret. The real residual is narrower: two authors on different *installed* versions still emit byte-different Markdown, which a snapshot corpus over `canonicalizeMarkdown` would surface as a reviewable CI diff. Worth doing at the next bump; not a pinning problem.
- **"All 35 cases pin `login: 'octocat'`" — wrong twice.** The file has 34 tests, and `login: 'octocat'` appears 6 times. The underlying point (O-6) stands; the evidence cited for it did not.
- **"Guard `ui/publish-payload.ts` by adding `jsonToMarkdown` to `FORBIDDEN_BINDINGS`" — self-defeating.** That file is already inside the guarded set (`COLLABORATION_PATH` pattern 3, `/^ui\/(node-id|publish|collab)[^/]*\.tsx?$/`), and `publish-payload.ts:246` calls `jsonToMarkdown` because emission is its job. Adding the binding turns the suite red on the correct implementation. An emitter guard, if ever needed, must name the *future* module rather than the binding.
- **"Comment-cache deletion has no story; no unit does it" — half wrong.** `cache-lifecycle.ts:54` and `:83` implement it and are tested. The gap is callers, not code. Folded into O-7.
- **"frontmatter is not published (`metadataMode: 'none'`)" — overtaken by `06df41f`.** `publish-payload.ts:245` now prepends a serialized frontmatter block; `metadataMode: 'none'` still suppresses *Luthor's own* envelope, which is a different thing. `frontmatter.title` winning remains settled.

### Standing constraints

- **The ~400-line in-repo Markdown emitter stays unbuilt.** It is the escape hatch if unattended/CI publish is ever required — trigger is a publish with no human at a keyboard (scheduled publish, bot-authored docs, CI-gated merge), not a vague "someday". If built it must never sit on the publish path, since a byte-equality veto turns every Luthor upgrade into an outage: CI differential test only, using `canonicalizeMarkdown` (`ui/luthor-bridge.ts:45`) as the oracle. See the third correction above for why the existing guard cannot simply be extended to cover it.
- **No retry, backoff, or `Retry-After` anywhere.** `github-executor.ts` punts it to the CLI, but the executor is deliberately buffered and header-blind, so rate-limit headers are structurally unobservable — while `credentials.ts` already parses the full header map for `x-oauth-scopes` and discards them. Batching is *not* the fix: there is no per-comment fan-out (the projection maps in memory, cost is O(pages)); U-4 and O-4 are the real sources. Accepted; revisit if throttling is observed.
- **`frontmatter.title` wins** (`document-protocol.ts`, four tests) — settled, do not reopen. Flipping the precedence would silently retitle every existing document.
